# 效率 / 效能筆記（Performance Notes）

這份文件記錄 renderer 卡頓的診斷歷程、已做的修正、尚未處理的成本，以及**下次再變慢時的排查方法**。

> TL;DR：主凶是 ActivityIndicator 的 CSS 動畫在無限迴圈裡動 `box-shadow`，把 renderer 主執行緒的重繪塞爆（66–88% CPU），連帶讓打字 / 注音組字很卡。改成只動 `opacity` + `transform` 後 renderer 掉到 ~5%。

---

## 1. 症狀

- 「越 run 越慢、很卡」，尤其**打字慢、拼音輸入中文很卡**。
- 環境：5 個 workspace、**20 個終端全部常駐掛載**（`peter` branch 的設計，切 workspace 不卸載以保留狀態）。
- Apple Silicon（M2）。

## 2. 已修正（本 branch 的 3 個 commit）

| commit | 內容 | 成效 |
|--------|------|------|
| `9124236` | **停止對 box-shadow 做動畫**（`.activity-indicator` 的 breathe keyframes 只留 `opacity`+`transform`）| **renderer 66–88% → ~5%**，打字/IME 卡頓解除 |
| `548cbb9` | 終端輸出持久化（緩衝放**主行程**，掛載時 replay），撐過 renderer reload / re-mount；scrollback 10000→1000 | 省記憶體、reload 後保留內容 |
| `1b4572a` | 輸出 fan-out 路由器（單一 dispatcher，O(1) 分發）；activity time 移出 immutable state（每 chunk 不再重建整陣列）；合併 setInterval 成共用 ticker；preview cache 抽模組並在關閉時清理；MutationObserver 加「值沒變不寫」防自我觸發 | 降低每 chunk / 每秒的固定開銷 |

### 主凶詳解（最重要）
`src/styles/main.css` 的 `@keyframes breathe-active` / `breathe-inactive` 原本在 `infinite` 迴圈裡動 **`box-shadow`**（模糊半徑一直變）。
- 動 `box-shadow`（或 `filter`、任何**模糊/陰影**）= 瀏覽器**每一幀在主執行緒重新光柵化** → 無法交給 GPU 合成。
- 一個終端 header + 一個 thumbnail 各有一個指示點，20 終端 ≈ **40 個指示點同時跑這動畫** → 主執行緒被重繪佔滿。
- 主執行緒忙 → 鍵盤 / IME 組字事件**排隊** → 打字卡。
- **教訓：CSS 動畫只動 `opacity` 和 `transform`（GPU 合成、不重繪）。** 靜態 `box-shadow` 放著沒關係，別放進 keyframes。

## 3. 目前基準（committed 程式碼、乾淨重啟後）

- renderer 靜止 **~5%**，有終端輸出時尖峰 ~16–20%。
- GPU 4–10%。main / vite 幾乎 0。
- 對照起點：box-shadow bug 時 renderer **66–88% 恆定**。

---

## 4. 尚未處理的次要成本（之後再慢可從這裡下手）

依「投報率」排序：

1. **xterm 用 DOM renderer（最慢的繪製路徑，沒載 WebGL）。**
   - 某終端**狂噴輸出**時（build / dev server / Claude 串流），繪製全壓在主執行緒 → 尖峰、可能卡。
   - 解法：`@xterm/addon-webgl`，把繪製交給 GPU（M2 是 Metal 加速，有效）。
   - **坑：Chromium 同時存在的 WebGL context 上限約 16 個**，20 終端全開 WebGL 會超限、context lost、反覆重建。
   - **正確做法：只給「當前可見的 active 終端」用 WebGL，切換時 attach/dispose**；其他 19 個 `visibility:hidden` 的維持 DOM。同時只需 1 個 context。
2. **20 個終端全部掛載 → 每次 `store.notify()` 重繪整棵樹。**
   - 實測：2 次 notify 連帶觸發近 400 個元件 render（互動時的小卡）。
   - 隱藏用 `visibility:hidden`（見 `main.css` `.terminal-wrapper.hidden` / `.workspace-container.hidden`），元素**仍參與 layout、xterm 仍照常繪製**，只是不 paint。
   - 解法選項：改 `display:none`（瀏覽器跳過 layout+paint，但 xterm 尺寸會歸零、切回來要 refit）；或用 React memo 讓非 active 的 TerminalPanel 不隨 notify 重繪；或非 active 終端暫停寫入、切回來再 flush。
3. **41 個 ActivityIndicator 每秒各輪詢一次**（`activity.check`）。很便宜，但數量隨終端數線性成長。可改成事件驅動或降頻。
4. **持久化緩衝的 `appendOutput` 用 `Array.shift()` 修剪**（`electron/pty-manager.ts`）。碎片化嚴重（很多小 chunk）時 shift() 是 O(n)。目前在主行程、影響小；真要優化可改 ring buffer 或用單一字串批次裁切。

---

## 5. 下次變慢時：排查方法論（重要）

### 5.1 先定位是哪個「行程」在燒
```bash
# 列出 Electron 各角色 + CPU/MEM
for p in $(pgrep -f "MacOS/Electron"); do
  cmd=$(ps -o command= -p $p); role="MAIN"
  case "$cmd" in *--type=renderer*) role="RENDERER";; *--type=gpu*) role="GPU";; *--type=utility*) role="UTIL";; esac
  echo "$role pid=$p cpu=$(ps -o %cpu= -p $p|tr -d ' ')% rss=$(( $(ps -o rss= -p $p|tr -d ' ')/1024 ))MB"
done
```
- **CPU 幾乎都在 RENDERER** → 是 React / xterm / 瀏覽器繪製的問題（本案就是）。
- 在 MAIN → 是 pty / IPC 問題。

### 5.2 關鍵觀念：JS 計數器看不到「繪製成本」
- 瀏覽器的 **paint / layout / composite 不在 JavaScript 裡**，任何 `console.time` / 計數器都量不到。
- **判斷法：renderer CPU 很高，但你插樁的 JS 耗時（ms）幾乎是 0 → 幾乎可斷定是 paint / layout，不是腳本。** 這時往 CSS 動畫、`visibility` 隱藏但仍在繪製、xterm renderer 這些方向找。

### 5.3 決定性工具：DevTools Performance profiler
- app 在 dev 模式會自動開 DevTools（`electron/main.ts` 的 `openDevTools()`）。
- Performance 分頁 → 錄 3–5 秒 → 看火焰圖裡 **Scripting（黃）/ Rendering（紫）/ Painting（綠）** 哪塊大。這是「到底花在哪」最終答案，不用猜。

### 5.4 想看 renderer 的 console.log（例如自己加的計數）
- Electron 的 renderer `console.log` **預設只進 DevTools、不進 stdout**，`tee` 抓不到。
- 用環境變數把它轉發到 stdout：
  ```bash
  ELECTRON_ENABLE_LOGGING=1 npm run dev 2>&1 | tee /tmp/agent-terminal-dev.log
  ```
- 自己加臨時計數時，**log 成純字串（JSON.stringify），不要 log 物件**——物件經 `ELECTRON_ENABLE_LOGGING` 轉發會變成 `[object Object]`。
- ⚠️ **觀測者效應**：全套逐 chunk / 逐 render 的計時計數本身會吃 CPU（本案 stale 計測讓 renderer 多了十幾 %）。量基準前記得移除計測並乾淨重啟。

### 5.5 CSS / HMR 陷阱
- 改 CSS 後 Vite 的 **CSS 熱抽換有時不會套進正在跑的實例**；量不到效果時，先在 app 視窗 **`Cmd+R` 硬重載**再量（pty 存活、狀態會延續）。
- 改 `electron/` 底下的檔案會觸發 `vite-plugin-electron` **自動重啟 Electron**（`main.ts` 改動重啟行程、`preload.ts` 改動 reload）。連續猛存會產生**互相重疊、被 launchd 收養的孤兒行程**（`ppid=1`、沒有 renderer 子行程）。診斷 renderer 時盡量只改 `src/`。

### 5.6 清掉孤兒 Electron（只殺孤兒、不誤傷正在用的實例）
```bash
# 只殺「父行程是 launchd(1)」的 Electron 主行程；健康實例父行程是 vite/node
for p in $(pgrep -f "MacOS/Electron"); do
  case "$(ps -o command= -p $p)" in *--type=*) continue;; esac   # 跳過 helper
  [ "$(ps -o ppid= -p $p | tr -d ' ')" = "1" ] && kill -9 "$p" && echo "killed orphan $p"
done
```
判斷：**孤兒 = `ppid=1` 且沒有 renderer/gpu 子行程；健康 = 父行程是 vite、且有 gpu+renderer+utility helper。**

---

## 6. 本次調查的關鍵教訓（給未來的自己）

1. **CSS 動畫只動 `opacity` / `transform`。** 動 `box-shadow` / `filter` / `width` / `top` 之類會逼主執行緒每幀重繪；元素一多就爆。
2. **renderer CPU 高但 JS 計時近 0 → 是 paint/layout，不是腳本。** 別在 JS 裡繼續找。
3. **`visibility:hidden` ≠ `display:none`**：前者元素仍 layout、xterm 仍繪製（只是不 paint）；後者才真正跳過。
4. **量效能前先移除計測並乾淨重啟**，否則觀測者效應會誤導。
5. **打字 / IME 卡的常見根因是「主執行緒被別的東西佔滿，輸入事件排隊」**，未必是輸入處理本身有問題——先看 renderer 整體 CPU。
